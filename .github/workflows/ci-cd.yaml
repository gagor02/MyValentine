name: CI/CD Pipeline - Trabajo Final

on:
  push:
    branches:
      - main

jobs:
  build-test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🛎️ Descargar código
        uses: actions/checkout@v4

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 🧪 Ejecutar Pruebas (Unit Tests)
        run: npm run test

      - name: 🐳 Login en Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      - name: 🚀 Build y Push de la Imagen Docker
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: gagor0202/myvalentine:latest, gagor0202/myvalentine:${{ github.run_number }}

      # --- ¡AQUÍ EMPIEZA LA MAGIA PRO! ---
      - name: 💻 Desplegar Automáticamente en el Servidor
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_IP }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          # Desactivamos comprobaciones estrictas para evitar errores de primera conexión
          script_stop: true
          envs: GITHUB_RUN_NUMBER
          script: |
            echo "Conectado al servidor. Reiniciando Kubernetes..."
            # Forzamos a Kubernetes a bajar la nueva imagen
            kubectl rollout restart deployment/react-app-deployment
            # Esperamos a que termine para reportar éxito
            kubectl rollout status deployment/react-app-deployment
            echo "¡Despliegue exitoso, Capitán!"