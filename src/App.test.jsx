import { describe, it, expect } from 'vitest';

describe('Validación del Trabajo Final', () => {
  it('Debe pasar las pruebas de CI/CD', () => {
    expect(1 + 1).toBe(2);
  });
});

// Me quede en el paso 3 de gemini